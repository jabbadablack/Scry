#include <engine/renderer/renderer.h>

extern "C" {

// This file is currently minimal as most logic is in render_system.cpp
// but it serves as the entry point for the renderer library.

} // extern "C"
